# agents
Agent swarm for practical engineering using LLMs and more

#make sure you have cloned and installed llmide from: https://github.com/lvwilson/llmide

pip install -r requirements.txt

#PEP workaround
#pip install --break-system-packages -r requirements.txt

#Set API keys as follows by adding the following line in bashrc (linux)
export CLAUDE_API_KEY='your_api_key'
