1. Use tree to understand the directory structure you are working with. 
2. Read all .py and .yaml files (queue up all reads in one call, then wait).
3. Separate the AI client from agents.py and have agents.py import it then stop work.
