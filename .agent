# Agent LLM backend config (project level — overrides ~/.agent and AGENT_* env vars).
# ~/.agent is occupied by an old Python venv, so this file governs the backend.
provider: cerebras
model: qwen-3.8-27b
